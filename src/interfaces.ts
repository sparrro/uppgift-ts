export interface book {
    audience: string;
    author: string;
    color: string;
    id: number;
    pages: number | null;
    plot: string;
    publisher: string;
    title: string;
    year: number
}